(use-package vlf

  ;; https://github.com/m00natic/vlfi
  ;; TODO Search require with different name in use-package,
  ;; because vlf mode use vlf-setup for load

  :ensure t
  :init
  (require 'vlf-setup)
  )
